services:
  dev-db:
    image: postgres:16-alpine
    ports:
      - 5434:5432
    environment:
        POSTGRES_USER: postgres
        POSTGRES_PASSWORD: postgres
        POSTGRES_DB: nest
    networks:
      - neidandev
networks:
  neidandev:
